# 학교생활기록부 학생별 내용 동일성 검토 통합 인수인계서

본 문서는 학교생활기록부 출력용 엑셀 파일을 자동 정제한 뒤 학생별 기재 내용의 동일성을 검토하는 프로그램의 운영 기준과 결과 해석 방법을 설명합니다. 프로그램은 업로드된 엑셀의 제목과 헤더를 분석하여 교과, 창의적체험활동, 자유학기활동, 행동특성 영역을 자동 인식합니다. 사용자는 정제 과정의 중간 상태가 아닌 **학생별 동일성 검토 결과**만 화면과 엑셀에서 확인합니다.

> **개인정보 보호 원칙**: 원본 학생부 파일과 검토 결과에는 개인정보가 포함될 수 있습니다. 원본 엑셀·정제 결과·검토 결과는 GitHub에 올리지 않으며, 기관의 개인정보 처리 지침에 따라 저장·공유해야 합니다. 성명 열은 원본 행의 연속성 판단에만 사용하며 결과에는 포함하지 않습니다.

## 1. 자동 정제 대상과 내부 표준 구조

프로그램은 검토에 앞서 출력용 엑셀의 반복 머리글·푸터를 제거하고, 페이지 경계를 넘어 분할된 학생의 내용을 하나의 행으로 병합합니다. 파일명이나 사용자의 수동 영역 선택은 필요하지 않습니다.

| 정제 대상 | 자동 판별 기준 | 내부 표준 열 |
| :--- | :--- | :--- |
| 교과 세부능력 및 특기사항 | `세부능력 및 특기사항` 헤더 | `학년 반`, `번호`, `과목`, `세부능력 및 특기사항` |
| 창의적체험활동상황 | `창의적체험활동상황` 제목 | `학년 반`, `번호`, `영역`, `특기사항` |
| 자유학기활동상황 | `자유학기활동상황` 제목 | `학년 반`, `번호`, `영역`, `특기사항` |
| 행동특성 및 종합의견 | `행동특성 및 종합의견` 헤더 | `학년 반`, `번호`, `행동특성 및 종합의견` |

비교과 영역은 원본의 `영역` 열을 직접 읽기 때문에 자율·자치활동, 동아리활동, 진로활동, 주제선택활동, 진로탐색활동 등 여러 활동 영역을 자동으로 처리합니다. 자유학기 주제선택활동은 `(활동명)(시간)` 형식 앞에 줄바꿈을 적용하여 활동별 내용을 구분합니다.

## 2. 동일성 검토 범위

동일성 검토의 단위는 **학생별 전체 내용**입니다. 프로그램은 서로 다른 과목 또는 활동 영역을 비교하지 않습니다. 교과 세부능력은 같은 과목끼리, 창체·자유학기 특기사항은 같은 활동 영역끼리, 행동특성은 행동특성 내용끼리 비교합니다. 문장 분리는 마침표(`.`), 물음표(`?`), 느낌표(`!`)를 기준으로 하며, 완전 동일과 유사도 계산 전에는 연속 공백을 하나로 정리합니다.

| 검토 구분 | 비교 대상 | 목적 |
| :--- | :--- | :--- |
| 파일별 검토 | 같은 업로드 파일·같은 과목 또는 활동 영역의 서로 다른 학생 | 한 학급 또는 한 파일 안의 유사 기재 내용을 확인합니다. |
| 교차 검토 | 먼저 업로드한 기준 파일과 그 뒤에 업로드한 파일의 같은 과목 또는 활동 영역 학생 | 여러 반 또는 여러 파일 사이의 유사 기재 내용을 확인합니다. |

## 3. 변경된 동일성 검토 기준

검토 결과는 개별 문장만 독립적으로 판정하지 않고, 두 학생의 **전체 내용 관계**를 우선 판단합니다. 기준은 아래와 같습니다.

| 우선순위 | 검토 기준 | 판정 방식 | 표시 색상 |
| :---: | :--- | :--- | :--- |
| 1 | 전체 내용 완전 동일 | 두 학생의 전체 내용에 포함된 모든 문장이 같은 순서와 내용으로 완전히 일치할 때 검출합니다. | **빨간색 계열** `#C00000` |
| 2 | 연속 3문장 이상 동일 | 두 학생의 내용 중 순서가 유지된 붙어 있는 연속 문장이 **3개 이상** 완전히 일치할 때 해당 문장 구간을 검출합니다. | **오렌지색 계열** `#ED7D31` |
| 3 | 전체 내용 유사도 95% 이상 | 완전 동일이 아닌 두 학생의 전체 내용에 대해 문자 단위 `SequenceMatcher` 유사도가 95% 이상일 때 검출합니다. 조사, 부사, 활동명 등 일부 표현만 달라진 사례를 우선 점검하기 위한 기준입니다. | **파란색 계열** `#1F4E78` |

**전체 내용 완전 동일**로 판정된 학생 쌍은 연속 3문장 이상 동일과 전체 내용 유사도 95% 이상 기준으로 추가 분류하지 않습니다. 즉 완전 동일 사례는 빨간색 결과만 남기고, 오렌지색·파란색 결과 열과 검출 기준에서는 제외합니다. 완전 동일이 아닌 경우에만 연속 3문장 이상 동일과 전체 내용 유사도 95% 이상을 추가로 판정합니다.

## 4. 학생 번호 표시 방식

파일별 검토 결과의 마지막 열은 `동일 문장 사용 학생 번호`입니다. 이 열에는 기준 학생을 포함해 같은 검토 기준으로 연결된 학생 번호를 오름차순으로 표시합니다. 예를 들어 3번, 4번, 6번, 10번 학생이 같은 검출 내용으로 연결되면 `3, 4, 6, 10번`으로 표시됩니다.

교차 검토는 반드시 먼저 업로드한 파일을 기준으로 표시합니다. 기준 파일의 학생 행만 결과 표에 나타나며, 마지막 열 `교차 동일 문장 사용 학생 번호`에는 후속 업로드 파일에서 검출된 학생의 학년·반과 번호를 표시합니다. 예를 들어 후속 파일의 4반 6번, 8번, 11번, 15번이 연결되면 `4반 6, 8, 11, 15번` 형태로 표시됩니다.

| 검토 구분 | 기준 행 | 마지막 열 | 표시 예시 |
| :--- | :--- | :--- | :--- |
| 파일별 검토 | 해당 파일의 검출 학생 행 | `동일 문장 사용 학생 번호` | `3, 4, 6, 10번` |
| 교차 검토 | 첫 업로드 파일의 검출 학생 행 | `교차 동일 문장 사용 학생 번호` | `4반 6, 8, 11, 15번` |

## 5. Streamlit 화면 구성

웹 앱은 정제 처리 상태, 정제 결과 요약, 정제 결과 미리보기 및 정제 결과 다운로드를 표시하지 않습니다. 사용자가 확인하는 화면은 문장 동일성 검토를 위한 파일별 탭, 교차 검토 탭, 그리고 색상 포함 검토 결과 엑셀 다운로드만으로 구성됩니다.

| 화면 요소 | 기능 |
| :--- | :--- |
| 파일 업로드 | 여러 `.xlsx` 파일을 업로드합니다. 파일을 선택한 순서가 교차 검토 기준 파일의 순서가 됩니다. |
| 색상 포함 검토 결과 다운로드 | 동일성이 검출된 행만 포함하는 하나의 검토 엑셀 파일을 생성합니다. |
| 파일 검토 탭 | 업로드 파일별로 생성되며, 같은 파일 안의 학생 전체 내용을 비교합니다. |
| 교차 검토 탭 | 두 파일 이상 업로드할 때 생성되며, 첫 업로드 파일을 기준으로 후속 파일을 비교합니다. |
| 동일성 검출 행만 표시 | 기본 선택 상태입니다. 해제하면 검출되지 않은 학생 행까지 검토 표에 표시합니다. |
| 검토 결과 색상 미리보기 | 결과 표의 `검토 내용 색상 미리보기` 열에서 빨강·오렌지·파랑으로 표시된 검출 구간과 검출 기준을 한 화면에서 즉시 확인합니다. |
| 표 형태의 상세 데이터 보기 | 필요할 때만 펼쳐서 기준별 결과 열을 포함한 일반 표 데이터를 확인합니다. |

## 6. 검토 결과 엑셀 파일 구조

`색상 포함 검토 결과 다운로드 (.xlsx)` 버튼은 동일성이 검출된 행만 포함하는 하나의 엑셀 파일을 생성합니다. 파일에는 `파일별 검토 1`, `파일별 검토 2` 등 업로드 순서에 따른 파일별 시트와 `교차 검토` 시트가 생성됩니다. **검토 결과 표와 엑셀 본문에는 원본 파일명과 시트 열을 표시하지 않습니다.**

`검토 내용` 열은 원문을 보존하며 검출 기준에 맞는 문장 또는 문장 구간을 색상으로 표시합니다. 기준별 결과 열에도 동일한 색상 계열을 반영하여, 필터나 검색을 통해 검토 이유를 빠르게 확인할 수 있습니다.

| 엑셀 열 | 내용 | 색상 처리 |
| :--- | :--- | :--- |
| 검토 대상 영역 | 교과목 또는 활동 영역 | 기본 색상 |
| 학년 반, 번호 | 기준 학생 식별 정보 | 기본 색상 |
| 검토 내용 | 학생의 전체 기재 내용 | 원문 내부 검출 구간을 기준별 색상으로 표시 |
| 전체 내용 완전 동일 | 전체 내용이 모두 같은 사례의 전체 문장 | 빨간색 계열 |
| 연속 3문장 이상 동일 | 붙어 있는 3문장 이상 공통 구간 | 오렌지색 계열 |
| 전체 내용 유사도 95% 이상 | 전체 내용 유사도 95% 이상 사례의 전체 문장 및 유사도 | 파란색 계열 |
| 동일 문장 사용 학생 번호 | 파일별 또는 교차 검토의 연결 학생 번호 | 기본 색상 |

## 7. 성능 최적화: 해시 기반 필터링과 2단계 비교

동일성 검토는 같은 과목 또는 활동 영역 안에서 학생 수가 늘수록 가능한 학생 쌍의 수가 빠르게 증가합니다. 이를 줄이기 위해 현재 엔진은 모든 학생 쌍에 정밀 유사도 계산을 실행하지 않고, **해시 기반 후보 탐색**과 **정밀 비교**를 분리한 2단계 구조를 사용합니다.

| 단계 | 처리 방식 | 정밀 비교 실행 여부 |
| :---: | :--- | :--- |
| 1단계: 전체 내용 해시 | 공백을 정규화한 전체 내용을 SHA-256 해시로 묶습니다. 동일 해시 학생만 완전 동일 후보가 됩니다. | 해시가 같은 학생 쌍만 확인 |
| 1단계: 연속 3문장 해시 | 각 학생의 붙어 있는 3문장 묶음을 해시화합니다. 같은 문장 창 해시가 있는 학생만 연속 문장 후보가 됩니다. | 같은 문장 창 해시가 있을 때만 실행 |
| 1단계: 토큰·길이 필터 | 2글자 이상 공통 토큰을 인덱스로 만들고, 전체 내용 길이만으로 유사도 95% 도달이 불가능한 쌍은 제외합니다. 지나치게 넓은 공통 토큰 버킷도 제외합니다. | 공통 토큰·길이 조건을 통과할 때만 실행 |
| 2단계: 정밀 비교 | 1단계 후보에만 문장 배열 비교와 전체 내용 `SequenceMatcher` 유사도 계산을 수행합니다. | 후보 쌍에 한정 |

전체 내용 완전 동일 학생 쌍은 해시 단계에서 바로 찾아 빨간색 결과로 처리합니다. 이 쌍은 후속 연속 3문장 및 유사도 95% 정밀 비교 대상에서 제외하므로, 판정 기준의 배타성도 함께 유지됩니다.

성능 검증용 240명 동일 영역 테스트에서는 전체 가능한 비교 쌍 28,680개 중 전체 내용 해시 후보 1개와 정밀 비교 후보 2개만 남았고, 28,677개 쌍을 정밀 비교 전에 제외했습니다. 실제 파일의 후보 수는 기재 내용의 공통성에 따라 달라지지만, 일반적으로 서로 다른 학생 내용이 많을수록 정밀 유사도 계산 횟수가 크게 줄어듭니다.

## 8. 파일 구성

| 파일 | 역할 |
| :--- | :--- |
| `clean_all_records.py` | 원본 양식 자동 판별, 반복 헤더·푸터 제거, 페이지 분할 내용 병합 |
| `similarity_review.py` | SHA-256 전체 내용·연속 3문장 해시 인덱스, 토큰·길이 후보 필터, 학생별 전체 내용 정밀 비교, 색상 Excel 생성 |
| `app.py` | 파일별 검토 탭, 교차 검토 탭, 색상 미리보기 및 검토 엑셀 다운로드 화면 |
| `requirements.txt` | Streamlit, pandas, openpyxl, XlsxWriter 등 실행 패키지 목록 |
| `학교생활기록부교과·비교과·행동특성데이터정제통합인수인계서.md` | 본 운영·인수인계 문서 |

## 9. 실행과 GitHub 배포

로컬 환경에서 앱을 실행하려면 프로젝트 폴더에서 아래 명령을 실행합니다.

```bash
pip install -r requirements.txt
streamlit run app.py
```

GitHub 저장소에는 코드와 문서만 추가합니다. 원본 학교생활기록부 엑셀과 검토 결과 엑셀은 저장소에 올리지 않습니다.

```bash
git init
git add app.py clean_all_records.py similarity_review.py requirements.txt 학교생활기록부교과·비교과·행동특성데이터정제통합인수인계서.md
git commit -m "학생별 전체 내용 동일성 검토 기준 수정"
git branch -M main
git remote add origin https://github.com/사용자아이디/저장소이름.git
git push -u origin main
```

Streamlit Community Cloud에서는 GitHub 저장소의 `main` 브랜치와 `app.py`를 시작 파일로 지정해 배포합니다. 코드 수정 후 GitHub에 `push`하면 웹 앱도 갱신됩니다.

## 10. 검증 결과와 유의 사항

변경된 검토 기준은 결정적 테스트 데이터로 검증했습니다. 전체 내용 완전 동일 사례는 빨간색 기준으로만 검출되고 연속 3문장·유사도 95% 이상 기준에는 포함되지 않는 것을 확인했습니다. 서로 다른 마지막 문장을 제외하고 붙어 있는 3개 문장이 같은 사례는 오렌지색 기준으로, 한 글자 차이를 포함하는 전체 내용은 파란색 유사도 기준으로 각각 검출했습니다. 또한 240명 동일 영역 성능 테스트에서 해시·토큰 필터가 28,680개 전체 가능 쌍 중 28,677개를 정밀 비교 전에 제외하는 것을 확인했습니다. 파일별 번호 집계, 먼저 업로드한 기준 파일 우선 교차 검토, 원본 파일명·시트 열을 제외한 색상 엑셀 생성도 정상 확인했습니다.

문장 분리는 마침표, 물음표, 느낌표를 기준으로 합니다. 문장 끝 문장부호가 없는 장문은 하나의 문장으로 처리될 수 있습니다. 유사도 95% 이상은 의미 기반 판정이 아니라 문자 배열 기반의 정량 지표이므로, 의미는 비슷하지만 표현이 크게 다른 문장은 검출되지 않을 수 있습니다. 반대로 철자 한두 글자, 조사, 부사 또는 활동명 차이는 검출될 수 있으므로, 결과는 최종 판단이 아니라 검토 우선순위를 정하는 보조 자료로 사용해야 합니다.

---

**개정 일자**: 2026년 8월 13일  
**작성자**: Manus AI
